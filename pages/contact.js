import React from 'react';

const Contact = (props) => {
    const anyVar = 'This is the Contact Page';
    return (
        <div>
            <h1>{anyVar}</h1>
        </div>
    );
};

export default Contact;