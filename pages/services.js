import React from 'react';

const Services = (props) => {
    const anyVar = 'This is the Service Page';
    return (
        <div>
            <h1>{anyVar}</h1>
        </div>
    );
};

export default Services;